# Signaling Server Configuration

# Server port (default: 5000)
PORT=5000

# Server host (0.0.0.0 allows external connections, localhost for local only)
HOST=0.0.0.0

# CORS origin (* allows all, set specific domain for production)
CORS_ORIGIN=*

# Socket.IO namespace (default: /remote-ctrl)
NAMESPACE=/remote-ctrl

# Enable detailed logging (true/false)
ENABLE_LOGGING=true

# Maximum number of rooms (0 = unlimited)
MAX_ROOMS=0

# Room timeout in milliseconds (0 = no timeout)
ROOM_TIMEOUT=0
